#
config:
  steps:
    - record:
        name: record test local-shell
        method: local-shell
        commands:
          - 'pip3 freeze'
  pdfOutput:
    template: "templates/template2.tex"
    author: SL 