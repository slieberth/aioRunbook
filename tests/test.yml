#
config:
  description : ""
  expected : ""
  preparation : ""
  workingDir: ./results_tests
  vars:
    text1: varTest
    text2: freeze
  steps:
    - record:
        name: '{{text1}}'
        method: local-shell
        commands:
          - 'pip3 {{text2}}'
  pdfOutput:
    template: "./template_v3.tex"
    author: SL 